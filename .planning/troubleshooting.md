## 확인된 이슈
* 조치된 내용은 체크박스를 추가하여 마킹할 것. 대괄호 안에 확인 필요라고 되어있으면 기존에 조치했으나 여전히 문제가 있는 경우라고 사용자가 확인한 내용임.


## 기능 추가/개선
- [x] 검색 품질 개선
    - 이미지 검색 테스트를 위해 샘플 이미지들을 몇개 추가하여 검색하였으나 검색 결과 창에 연관 이미지가 잘 걸리는 것 같지 않음. 샘플 이미지 파일들은 "C:\Users\hdg99\Documents\사진들" 폴더에 있고, 테스트 용이성을 위해 파일 제목을 유의미하게 설정하였음.
    * ex) '강아지'라고 검색하였으나 '사람들2.png', '자동차2.jpg' 같은 결과가 상위에 나옴.
        1) 임베딩 모델의 성능이 안좋은 것인지 테스트 하여 결과 리뷰 필요 [x]
            - 테스트 결과 `Qwen/Qwen3-VL-Embedding-2B` 모델의 텍스트-이미지 간 코사인 유사도 연산 점수가 매우 낮거나(-0.05 ~ 0.03) 정답과 거리가 먼 결과가 상위 점수로 랭크됨.
        2) 1번의 결과를 바탕으로 모델 성능 때문인지, 우리의 검색 로직 결과로 보여주는 정렬 로직 때문인지 검증 필요. [x]
            - 검증 결과: 애플리케이션의 검색 및 정렬 로직(`vector_db.py`, `indexer.py`)은 정상이나, 모델 자체의 임베딩 정합성 부족으로 최종 검색 결과에 노이즈가 발생하고 있음.
        3) 1,2번의 결과를 종합하여 소스코드 레벨에서 보완할 부분이 있으면 개선 필요. [x]
            - 조치: 모델 문제가 아니라 `qwen_adapter.py` 내 `transformers/torchvision` 버그로 인한 무작위 가짜 벡터 문제, 미구현된 `apply_chat_template`, 시스템 `instruction`, `Last Token Pooling` 누락 등의 종합 오류였음을 확인하여 해결 완료. 원래 2B 파라미터 환경에서도 100% 정상 작동함을 테스트 (`test_embedding.py`) 및 조치 이력(`Patch.md`)에 저장함.

